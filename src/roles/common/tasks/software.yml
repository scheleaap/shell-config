---
- name: "Refresh apt cache"
  become: true
  apt:
    update_cache: true
- name: "Install common packages"
  become: true
  apt:
    name: "{{ item }}"
  with_items:
    - curl
    - wget
    - git
    - ssh
    - htop
    - joe
# - name: "Install Java"
#   become: true
#   apt:
#     name: "{{ item }}"
#   with_items:
#     - openjdk-8-jdk
#     - openjdk-8-jre
#     - icedtea-8-plugin
