[credential]
	helper = cache --timeout={{ git_cache_timeout }}
[user]
	email = {{ git_user_email }}
	name = {{ git_user_name }}
[push]
	default = simple
[alias]
	co = checkout
	br = branch
	ci = commit
	st = status
	unstage = reset HEAD --
[core]
	editor = atom --wait
